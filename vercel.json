{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)",     "dest": "/$1" }
  ],
"functions": {
    "api/report/submit-batch.js": { "maxDuration": 60 },
    "api/report/send.js":         { "maxDuration": 60 }
  }
}
